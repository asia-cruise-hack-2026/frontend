# macOS
.DS_Store

# Claude local settings (machine-specific)
.claude/settings.local.json

# Node (frontend, 향후)
node_modules/
dist/
build/
.env
.env.*
!.env.example

# 외부 참조 클론 (읽기 전용, 커밋 안 함) — README만 추적
refs/*
!refs/README.md
